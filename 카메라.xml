<?xml version="1.0" encoding="ks_c_5601-1987"?>
<카메라 trace="true">
  <!-- 최대 시야 거리(기본값: 1000.0) -->
  <view_distance 값="1152.0"/>

  <!-- 낮을 수록 안개가 줄어듬.(기본값: 2.55) -->
  <fog 값=""/>

  <!-- 이동할 수 있는 최소 x y좌표(기본값: 560.0) -->
  <x_min 값="0.0"/>

  <!-- 이동할 수 있는 최대 x좌표(기본값: 4480.0) -->
  <x_max 값="5120.0"/>

  <!-- 이동할 수 있는 최소 y좌표(기본값: 4580.0) -->
  <y_max 값="5120.0"/>

  <!-- 기본값: 0.39 -->
  <rotate_min 값="-1.570796"/>

  <!-- 기본값: 0.95 -->
  <rotate_max 값="1.570796"/>

  <!-- 확대 거리(기본값: 220.0) -->
  <zoom_in 값="32.0"/>

  <!-- cg품질 저 축소거리(기본값: 400.0) -->
  <zoom_out0 값="752.0"/>

  <!-- cg품질 중 축소거리(기본값: 500.0) -->
  <zoom_out1 값="852.0"/>

  <!-- cg품질 고 축소거리(기본값: 600.0) -->
  <zoom_out2 값="952.0"/>

  <!-- 지형 통과(기본값: false) -->
  <through 값="true"/>

  <!--
  보고 있는 좌표를 고정하지 않고 터치스크린을 컨트롤하듯 화면일 이동.
  카메라 좌표를 이동하려면 방향키를 사용.
  기본값: false
  -->
  <free 값=""/>

  <!-- 자유모드에서 x축 방향을 반대로(기본값: false) -->
  <reverse_x 값=""/>

  <!-- 자유모드에서 y축 방향을 반대로(기본값: true) -->
  <reverse_y 값=""/>
</카메라>